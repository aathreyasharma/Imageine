<% image_tag @pin.image.url(:medium) %>


<div class="panel panel-default center">
  <div class="panel-heading"><h1>Editing Pin</h1>
    <div class="panel-body">
        <%= image_tag @pin.image.url(:medium) %>
        <%= render 'form' %>
    </div>
    <div class="panel-footer"><%= link_to 'Show', @pin %> |
    <%= link_to 'Back', pins_path %>
    </div>